enum MainTab { home, favourite, qrScan, profile, basketEat, basketStuff }

class OrderType {
  static const String delivery = "delivery";
  static const String orderAt = "orderAt";
}

class ProductType {
  static const String food = "food";
  static const String stuff = "stuff";
}

enum BasketAddRemoveProduct { add, remove }
